name: one_fit_man
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.4.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  camera: ^0.10.6
  flutter_bloc: ^8.1.6
  google_mlkit_pose_detection: ^0.13.0
  permission_handler: ^11.3.0
  audioplayers: ^6.1.0
  settings_ui: ^2.0.2
  highlight_nav_bar: ^1.0.0
  geolocator: ^10.0.2
  confetti: ^0.8.0
  flutter_secure_storage: ^9.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
     - assets/pose_pushup.png
     - assets/pose_squat.png
     - assets/pose_done.wav
     - assets/pose_situp.png
     - assets/pose_running.png